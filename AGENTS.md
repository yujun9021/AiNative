# AI-native 개발 운영 규칙

## 판단 기준

내용이 충돌하면 다음 순서로 판단한다.

1. 사용자의 최신 결정
2. 승인된 `intent.md`
3. 승인된 `spec.md`
4. 승인된 `plan.md`
5. 현재 코드
6. 기록된 증거

## 단계별 승인

- `intent.md`는 목적과 범위를 설명한다. 사용자 승인 전에는 다음 단계로 가지 않는다.
- `spec.md`는 관찰 가능한 동작을 정의한다. intent 승인 후 작성한다.
- `plan.md`는 구현과 검증 방법을 정의한다. spec 승인 후 작성한다.
- plan 승인 전에는 제품 코드를 변경하지 않는다.
- `evidence.md`에는 명령, 결과, 리뷰, 사용자 승인을 기록한다.
- 상태는 `draft`, `approved`, `building`, `review`, `done`을 사실대로 사용한다.
- 로컬 검사를 커밋, CI, 배포, 사용자 승인과 동일하게 표현하지 않는다.

## 변경 원칙

- 한 변경에는 하나의 로드맵 항목만 다룬다.
- 승인된 plan을 만족하는 가장 작은 변경을 선택한다.
- 관련 없는 사용자 작업은 보존한다.
- 변경한 파일과 수행한 검증을 구분해 보고한다.
